# PostgreSQL Database Configuration (Docker Container)
spring.datasource.url=jdbc:postgresql://localhost:${DB_PORT:5432}/${DB_NAME:postgraduates_db}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD:admin}

# PostgreSQL specific settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop

# PostgreSQL-specific data initialization
spring.sql.init.data-locations=classpath:init-postgres.sql

# PostgreSQL specific properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
